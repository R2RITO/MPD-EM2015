 <?php

    $conn = oci_connect("miniproyecto", "m1n1pr0y3ct0", "127.0.0.1/XE");

    $stid = oci_parse($conn, 'insert into test values (11,22)');
    oci_execute($stid);


    $stid = oci_parse($conn, 'select col1, col2 from test');
    oci_execute($stid);

    $nrows = oci_fetch_all($stid, $results);

    echo "<html><head><title>Oracle PHP Test</title></head><body>";
    echo "<center><h2>Oracle PHP Test</h2><br>";
    echo "<table border=1 cellspacing='0' width='50%'>\n<tr>\n";
    echo "<td><b>Col1</b></td>\n<td><b>Col2</b></td>\n</tr>\n";

    for ($i = 0; $i < $nrows; $i++ ) {
      echo "<tr>\n";
      echo "<td>" . $results["COL1"][$i] . "</td>";
      echo "<td>$" . number_format($results["COL2"][$i], 2). "</td>";
      echo "</tr>\n";
    }

    echo "<tr><td colspan='2'> Number of Rows: $nrows</td></tr></table>";
    echo "<br><em>If you see data, then it works!</em><br></center></body></html>\n";

  ?>
